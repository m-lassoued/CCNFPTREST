{% extends 'base.html.twig' %}

{% block title %}Metier{% endblock %}

{% block body %}
    <h1>Metier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metier.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ metier.libelle }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ metier.code }}</td>
            </tr>
            <tr>
                <th>Actif</th>
                <td>{{ metier.actif ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('metier_index') }}">back to list</a>

    <a href="{{ path('metier_edit', {'id': metier.id}) }}">edit</a>

    {{ include('metier/_delete_form.html.twig') }}
{% endblock %}