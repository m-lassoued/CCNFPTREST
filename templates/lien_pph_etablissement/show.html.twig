{% extends 'base.html.twig' %}

{% block title %}LienPphEtablissement{% endblock %}

{% block body %}
    <h1>LienPphEtablissement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>DateDebut</th>
                <td>{{ lien_pph_etablissement.dateDebut ? lien_pph_etablissement.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ lien_pph_etablissement.dateFin ? lien_pph_etablissement.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lien_pph_etablissement_index') }}">back to list</a>

    <a href="{{ path('lien_pph_etablissement_edit', {'pph': lien_pph_etablissement.pph}) }}">edit</a>

    {{ include('lien_pph_etablissement/_delete_form.html.twig') }}
{% endblock %}