<form method="post" action="{{ path('lien_pph_etablissement_delete', {'pph': lien_pph_etablissement.pph}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lien_pph_etablissement.pph) }}">
    <button class="btn">Delete</button>
</form>